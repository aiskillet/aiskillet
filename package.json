{
  "name": "aiskillet",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AISkillet — a marketplace for AI skills, plugins, agents, and MCP servers.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
